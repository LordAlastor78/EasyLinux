# shellcheck shell=bash

readonly LOG_ROOT_DEFAULT="$HOME/.easylinux/logs"
readonly APT_PACKAGES_COMMON=(
  curl
  ca-certificates
  gnupg
  lsb-release
)

# Runtime defaults (can be overridden by CLI flags)
AUTO_YES=0
SKIP_UPDATE=0
RUN_ALL=0
PROFILE=""
